# Auto Factory

Sistema de uso interno para gerenciamento de Iot da fábrica.

Ele cria um banco de dados, uma api e um front-end feito para ser usado de forma completamente local, onde o servidor fica na mesma rede que os outros dispositivos.

para rodar:

```` Bash
docker compose up --build -d
````